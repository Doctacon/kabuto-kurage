Status: done
Created: 2026-06-18
Updated: 2026-06-18
Parent: .loom/tickets/2026-06-18-build-mini-engineering-intelligence-platform.md
Depends-On: .loom/tickets/2026-06-18-validate-delta-dagster-github-stack.md

# Scaffold Portfolio Data Platform

## Scope

Create the initial repository scaffold for a portfolio-quality Python data platform.

Expected areas:

- Source package layout.
- Tests layout.
- `uv` or selected Python package manager setup.
- Formatting/linting/type-checking configuration.
- Environment variable conventions.
- `.gitignore` entries for secrets, local data, and generated artifacts.
- Initial README skeleton.
- Developer commands for install, test, lint, and Dagster startup.

## Out of Scope

- Real GitHub ingestion.
- Real Delta table design beyond placeholders needed for structure.
- Full documentation polish.

## Acceptance Criteria

- A new developer can install dependencies with documented commands.
- Tests can be run locally.
- Lint/type-check commands exist or are intentionally deferred with rationale.
- Secret and local-data paths are ignored by git.
- README explains the project goal and current milestone.

## Current State

Done. The repository now has an initial Python data-platform scaffold using the validated stack choices: `uv`, Python 3.11+, `deltalake`, `pyarrow`, `dagster`, and `httpx`.

The scaffold includes:

- `pyproject.toml` with runtime and development dependencies.
- `uv.lock` generated by `uv sync`.
- `src/kabuto_kurage/` package layout.
- `tests/` layout with scaffold tests.
- Ruff, mypy, and pytest configuration.
- `.gitignore` coverage for secrets, local data, virtualenvs, caches, and generated runtime state.
- `.env.example` for local environment conventions.
- README and `docs/development.md` developer commands.
- A stable Dagster code-location module at `kabuto_kurage.definitions` with an intentionally empty `Definitions` object for downstream asset tickets.

Evidence: `.loom/evidence/2026-06-18-scaffold-validation.md`.

## Journal

- 2026-06-18: Set active and delegated implementation to worker.
- 2026-06-18: Added Python package scaffold, `uv` project config, developer tooling, README/development docs, `.gitignore`, and `.env.example`.
- 2026-06-18: Ran `uv sync`; dependency resolution, package build, and install succeeded.
- 2026-06-18: Ran `uv run pytest`, `uv run ruff check .`, and `uv run mypy src`; all passed.
- 2026-06-18: Verified Dagster code-location import with `uv run python -c "from kabuto_kurage.definitions import defs; print(type(defs).__name__)"`.
- 2026-06-18: Re-ran `uv run python tools/validate_stack.py`; Delta/Dagster proof still passed, GitHub token remained intentionally absent/skipped.
- 2026-06-18: Recorded scaffold validation evidence and moved ticket to done.

## Results

Acceptance criteria satisfied:

- A new developer can install dependencies with `uv sync`, documented in README and `docs/development.md`.
- Tests can be run locally with `uv run pytest` and currently pass.
- Lint/type-check commands exist with `uv run ruff check .` and `uv run mypy src`; both pass.
- Secret and local-data paths are ignored by git via `.gitignore`.
- README explains the project goal and the current scaffold milestone.

## Blockers

None for this scaffold ticket. Real GitHub ingestion, tenant configuration, Delta table design, and Dagster assets remain deferred to downstream tickets.
